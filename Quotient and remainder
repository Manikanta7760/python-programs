# python program to find the quotient and remainder of two numbers

dividend=int(input("Enter the dividend value:"))
divisor=int(input("Enter the divisor value:"))
if divisor==0:
    print("Error! cant find quotient and remainder")
else:
    quotient=dividend/divisor
    remainder=dividend%divisor
    print("Quotient =",quotient)
    print("Remainder =",remainder)

output:
Enter the dividend value:4
Enter the divisor value:2
Quotient = 2.0
Remainder = 0
